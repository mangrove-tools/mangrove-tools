{
  "name": "mangrove-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site, local AI Gateway smoke harness, and server-side analytics API dependencies.",
  "scripts": {
    "smoke": "node --env-file=.env.local index.mjs",
    "test:api": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.9",
    "ai": "^7.0.35"
  }
}
