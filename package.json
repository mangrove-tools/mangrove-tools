{
  "name": "mangrove-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site + local AI Gateway smoke test harness. The site itself is bundler-free vanilla HTML/CSS/JS; this manifest only exists to install the 'ai' SDK for the local smoke test in index.mjs.",
  "scripts": {
    "smoke": "node --env-file=.env.local index.mjs"
  },
  "dependencies": {
    "ai": "^7.0.35"
  }
}
